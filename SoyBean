local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/discord"))()

local Window = Library:Window("Ultimate Internet", "By Vqse", Color3.fromRGB(44, 47, 51), Enum.KeyCode.RightControl)

local ScriptsTab = Window:Server("Discord Townhall", "http://www.roblox.com/asset/?id=6031075938")

local MissPaintServer = Window:Server("Miss.Paint", "http://www.roblox.com/asset/?id=6031075938")
local UpioChannel = MissPaintServer:Channel("Upio")

UpioChannel:Button("Mspaint", function()
    loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/002c19202c9946e6047b0c6e0ad51f84.lua"))()
end)

UpioChannel:Button("Cobalt", function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/KingHub26/CosmicLoader/refs/heads/main/Universal%20Hub'))()
end)

UpioChannel:Button("Upio Discord", function()
    setclipboard("https://discord.com/invite/mspaint")
end)

local KeyChannel = MissPaintServer:Channel("Mspaint Key")

KeyChannel:Label("Tutorial for Key system")

KeyChannel:Button("MspaintKey.Docs", function()
    setclipboard("https://docs.mspaint.cc/key-system")
end)

KeyChannel:Button("Snatch a Key", function()
    setclipboard("https://www.mspaint.cc/key")
end)

local SolunaServer = Window:Server("Soluna", "http://www.roblox.com/asset/?id=6031075938")
local EndOfOverdosingChannel = SolunaServer:Channel("End of Overdosing")

EndOfOverdosingChannel:Button("Soluna", function()
    loadstring(game:HttpGet("https://soluna-script.vercel.app/main.lua", true))()
end)

EndOfOverdosingChannel:Button("EoO Discord", function()
    setclipboard("https://discord.gg/gdpCUVj6uS")
end)
local EtronixServer = Window:Server("Etronix", "http://www.roblox.com/asset/?id=6031075938")
local EtronixScriptChannel = EtronixServer:Channel("Uerd")

EtronixScriptChannel:Button("Etronix", function()
    loadstring(game:HttpGet("https://getetronix.eu/script"))()
end)

EtronixScriptChannel:Button("Discord", function()
    setclipboard("https://discord.gg/uBw6WhrHJw")
end)

local KeyCollectorChannel = EtronixServer:Channel("Key Collector")

KeyCollectorChannel:Button("Key Gen", function()
    setclipboard("https://ads.luarmor.net/get_key?for=key_1-sjtpbEzQAEPw")
end)

KeyCollectorChannel:Button("Key Purchasing", function()
    setclipboard("https://etronix.sellhub.cx/")
end)

local SkibidiToiletServer = Window:Server("Skibidi Toilet", "http://www.roblox.com/asset/?id=6031075938")
local HighauppChannel = SkibidiToiletServer:Channel("Highaupp")

HighauppChannel:Button("Script", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/aemos2/Skibidihub/refs/heads/main/SkibidiHUB.txt"))()
end)

HighauppChannel:Button("Discord", function()
    setclipboard("https://discord.gg/VAPWV3gsmn")
end)

local GameSpecificsChannel = ScriptsTab:Channel("Game Specifics")

local gameInfo = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId)
GameSpecificsChannel:Label("Game: " .. gameInfo.Name)
GameSpecificsChannel:Label("Creator: " .. gameInfo.Creator.Name)

local HttpService = game:GetService("HttpService")
local selectedScript = nil

local function fetchGameScripts()
    local success, result = pcall(function()
        local response = game:HttpGet("https://scriptblox.com/api/script/search?q=" .. HttpService:UrlEncode(gameInfo.Name))
        return HttpService:JSONDecode(response)
    end)
    
    if success and result.result and result.result.scripts then
        return result.result.scripts
    else
        return {}
    end
end

local scripts = fetchGameScripts()
local scriptOptions = {}
local scriptData = {}

for i, script in ipairs(scripts) do
    if i <= 20 then
        table.insert(scriptOptions, script.title or "Script " .. i)
        scriptData[script.title or "Script " .. i] = script
    end
end

if #scriptOptions > 0 then
    local scriptDropdown = GameSpecificsChannel:Dropdown("Available Scripts", scriptOptions, function(selected)
        selectedScript = scriptData[selected]
    end)
    
    local executeButton = GameSpecificsChannel:Button("Execute Script", function()
        if selectedScript and selectedScript.script then
            loadstring(selectedScript.script)()
        end
    end)
    
    local copyScriptButton = GameSpecificsChannel:Button("Copy Script", function()
        if selectedScript and selectedScript.script then
            setclipboard(selectedScript.script)
        end
    end)
else
    GameSpecificsChannel:Label("No scripts found for this game")
end

local PlayersListChannel = ScriptsTab:Channel("Players-Lists")

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local playerNames = {}
local selectedPlayer = nil
local viewToggle = nil
local copyButton = nil
local teleportButton = nil

for _, player in ipairs(Players:GetPlayers()) do
    table.insert(playerNames, player.Name)
end

PlayersListChannel:Label("Players Online: " .. #playerNames)

if #playerNames > 0 then
    local playerDropdown = PlayersListChannel:Dropdown("Player List", playerNames, function(selected)
        selectedPlayer = Players:FindFirstChild(selected)
    end)
    
    local viewToggle = PlayersListChannel:Toggle("View Player", false, function(enabled)
        if selectedPlayer and selectedPlayer.Character then
            local camera = workspace.CurrentCamera
            if enabled then
                camera.CameraSubject = selectedPlayer.Character.Humanoid
            else
                camera.CameraSubject = localPlayer.Character.Humanoid
            end
        end
    end)
    
    local copyButton = PlayersListChannel:Button("Copy Username", function()
        if selectedPlayer then
            setclipboard(selectedPlayer.Name)
        end
    end)
    
    local teleportButton = PlayersListChannel:Button("Teleport", function()
        if selectedPlayer and selectedPlayer.Character and localPlayer.Character then
            local targetPosition = selectedPlayer.Character.HumanoidRootPart.Position
            localPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(targetPosition)
        end
    end)
else
    PlayersListChannel:Label("No players online")
end

local ExploitsListChannel = ScriptsTab:Channel("Exploits-Lists")

local executors = {
    ["Zenith"] = "https://zenith.win/?__cf_chl_rt_tk=q_.3Bi04EQH1K534zpGG6Vgp0ordr03rkKjw8vlUEUo-1753659320-1.0.1.1-25e9fIYOsLQJ4KPaJilVOQDdMDK7haDcFJ_81Wq9Iko",
    ["Velocity"] = "https://getvelocity.live/",
    ["Swift"] = "https://getswift.vip/",
    ["Solara"] = "https://getsolara.dev/",
    ["Xeno"] = "not supported",
    ["Hydrogen"] = "https://www.hydrogen.lat/",
    ["Cryptic"] = "https://getcryptic.net/",
    ["Delta"] = "https://deltaexploits.gg/",
    ["Valex"] = "https://valex.io/"
}

local executorNames = {}
local selectedExecutor = nil
local copyLinkButton = nil

for name, _ in pairs(executors) do
    table.insert(executorNames, name)
end

local executorDropdown = ExploitsListChannel:Dropdown("Executor List", executorNames, function(selected)
    selectedExecutor = selected
end)

local copyLinkButton = ExploitsListChannel:Button("Copy Link", function()
    if selectedExecutor and executors[selectedExecutor] then
        if executors[selectedExecutor] ~= "not supported" then
            setclipboard(executors[selectedExecutor])
        end
    end
end)

local UtilitiesChannel = ScriptsTab:Channel("Utilities")

UtilitiesChannel:Button("Inf Yield", function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
end)

UtilitiesChannel:Button("Dex", function()
    loadstring(game:HttpGet("https://cdn.wearedevs.net/scripts/Dex%20Explorer.txt"))()
end)

UtilitiesChannel:Button("Remote Spy", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/exxtremestuffs/SimpleSpySource/master/SimpleSpy.lua"))()
end)
